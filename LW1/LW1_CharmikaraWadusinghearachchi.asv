% Charmikara Wadusinghearachchi
% EDIfu25/1
% 09/11/2026

x = 1:32;
y = x.^2;

plot(x, y, 'o-r', x, y/3, 'xb')
title('Dvi funkcijos')
xlabel('X-ai')
ylabel('F_1 [-o-]  |  F_2 [-x-]')


% --- COMPLEMENTARY TASK --
N = 2
V = N+1:0.5:N+4

A = reshape(N:N+8, 3,3)'
% matrix fills column by column, therefore we need to use '

optA = A(3,2)
optB = A(2:3, 1:2)
optC = A([1 3], [1 3])

Vmod = [V NaN NaN];
Vmod = reshape(Vmod, 3, 3);

AVc
